# Project conventions

- Keep the root and `client` package versions synchronized.
- Follow semantic versioning: increment the minor version for significant backward-compatible features and the major version for breaking changes. Patch releases may be used for fixes.
- Run `npm test` and the production build before committing release changes.
